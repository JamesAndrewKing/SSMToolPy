# MATLAB Reference Fixtures

This directory stores MATLAB/Octave-generated fixtures when available.

`low_level_reference.json` was generated with MATLAB R2024b for spot checks of
`nsumk` and `sub2multiind`. Other initial tests use deterministic fixtures
derived directly from the MATLAB source files listed in `docs/migration_report.md`.
